const compute = (mapProps) => {
    console.log("compute algorithms");
    // alert(JSON.stringify(mapProps.par_sex))
    if (mapProps.par_st === false )  {
        alert(JSON.stringify(mapProps.par_st));
    }
}

module.exports =  {
   compute
}

